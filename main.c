#include"func.h"

int main()
{
    char *buffer;
    int teach;
    char *in;
    char *out;
    char name[15];
    buffer = s_malloc(len);
    srand((unsigned) time (NULL));

    printf("what is your name? \n");
    scanf("%s",name);
    getchar();

    inputs(*buffer, name);
    in = s_malloc(buffer)+1;







    strcpy(in, buffer)

