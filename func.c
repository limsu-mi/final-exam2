#include<stdio.h>

char inputs(char *buffer, char *nugu);
char search_inputs(char *buffer, char in);
int s_malloc(int len);
int TEACH(int *in);
char ffgets(char *in, char *buffer);
char return_answer(char *buffer, char *out)
int nline(int buffer, int *sun);

char inputs(char *buffer, char *nugu)
{
    name != NULL;
    fprintf("%s", name);
}

int TEACH(int *in)
{
    if(strcmp(in, buffer)
    {
        return_answer(buffer, in);
    }

    else if
    {
        strcmp(in, "");
        TEACH(in);
    }
}

